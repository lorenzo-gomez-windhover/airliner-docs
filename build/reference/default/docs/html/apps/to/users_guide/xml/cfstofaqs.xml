<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.11">
  <compounddef id="cfstofaqs" kind="page">
    <compoundname>cfstofaqs</compoundname>
    <title>TO Application Frequently Asked Questions</title>
    <detaileddescription>
<para><heading level="1">General </heading>
</para><para><bold> (Q) What channels can be disabled and enabled? </bold> <linebreak/>
 <linebreak/>
 <emphasis> All the TO channels defined by the custom layer should startup and be enabled. </emphasis></para><para><bold> (Q) When a new configuration table is loaded for a channel, are the channel&apos;s priority queues and message flows cleared? </bold> <linebreak/>
 <linebreak/>
 <emphasis> When a new configuration table is loaded the channel&apos;s priority queues and message flows are cleared, also any queued Software Bus messages are dequeued. </emphasis></para><para><bold> (Q) Are the configuration tables all load ONLY, and dump files all dump ONLY? Are any Load/Dump? </bold> <linebreak/>
 <linebreak/>
 <emphasis> The TO dump tables are dump only. The TO configuration tables are dump and load. The TO configuration table is load(ed) with a particular configuration, but the table parameter values after it&apos;s loaded can be modified by a command. Then that updated version of that table can be dumped to a file. If the user likes those changes, that table can be reloaded. </emphasis> </para>    </detaileddescription>
  </compounddef>
</doxygen>
